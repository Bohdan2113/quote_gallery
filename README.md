# QuoteGallery Flutter App

Додаток для збереження та перегляду цитат, створений на Flutter.

## 📁 Структура проекту

```
lib/
├── main.dart                          # Точка входу в додаток
├── core/                              # Основні компоненти
│   ├── theme/
│   │   └── app_theme.dart            # Теми та стилі додатку
│   └── widgets/                       # Переиспользовані віджети
│       ├── custom_button.dart        # Стилізована кнопка
│       ├── custom_text_field.dart    # Стилізоване текстове поле
│       └── quote_card.dart           # Картка цитати
├── data/                              # Дані та моделі
│   ├── models/
│   │   └── quote_model.dart          # Моделі QuoteModel та UserModel
│   └── mock/
│       └── mock_data.dart            # Mock дані для демонстрації
└── presentation/                      # UI екрани
    └── screens/
        ├── auth/
        │   └── auth_screen.dart      # Екран авторизації/реєстрації
        ├── main/
        │   └── main_screen.dart      # Головний екран зі списком цитат
        ├── profile/
        │   └── profile_screen.dart   # Екран профілю користувача
        └── create/
            └── create_screen.dart    # Екран створення/редагування цитати
```

## 🎨 Особливості

### 1. **Чиста архітектура**
- Розділення на шари: `core`, `data`, `presentation`
- Переиспользовані компоненти у `core/widgets`
- Централізована тема у `core/theme`

### 2. **Екрани**

#### Екран авторизації (`auth_screen.dart`)
- Вкладки "Вхід" та "Реєстрація"
- Mock авторизація з вбудованими користувачами
- Валідація форм

#### Головний екран (`main_screen.dart`)
- Три вкладки: "Усі цитати", "Мої цитати", "Улюблені"
- Пошук по цитатам, авторам та тегам
- Фільтри по автору та тегу
- Сітка цитат з можливістю редагування/видалення своїх
- FAB кнопка для додавання нових цитат

#### Екран профілю (`profile_screen.dart`)
- Відображення інформації про користувача
- Кнопка виходу з акаунту

#### Екран створення (`create_screen.dart`)
- Форма для введення тексту цитати, автора та тегів
- Кнопки "Зберегти" та "Відмінити"

### 3. **Переиспользовані віджети**

#### `CustomButton`
- Підтримка primary/outlined стилів
- Можливість кастомізації кольорів

#### `CustomTextField`
- Стилізоване поле з label
- Підтримка багаторядкового введення
- Підтримка прихованого введення (пароль)

#### `QuoteCard`
- Картка з відображенням цитати
- Теги, автор
- Кнопка додавання до улюблених
- Кнопки редагування/видалення для власника

### 4. **Mock дані**

Для демонстрації додано:
- 2 користувачі з можливістю авторизації
- 4 цитати різних авторів з тегами
- Поточний користувач зберігається глобально

## 🚀 Запуск проекту

1. Встановіть Flutter SDK
2. Склонуйте проект
3. Виконайте команди:

```bash
flutter pub get
flutter run
```

## 📝 Mock користувачі для входу

- **Email:** `ivan@example.com`, **Пароль:** `password123`
- **Email:** `other@example.com`, **Пароль:** `qwerty`

## 🎯 Функціонал (заглушки)

На даний момент реалізовано тільки UI без реальної логіки:

- ✅ Авторизація (mock користувачі)
- ✅ Відображення цитат
- ✅ Пошук та фільтрація
- ✅ Додавання до улюблених (в пам'яті)
- ⏳ Створення/редагування цитат (заглушка)
- ⏳ Видалення цитат (заглушка)
- ⏳ Реальне збереження даних

## 📱 Адаптивність

Додаток адаптований під різні розміри екранів:
- GridView з автоматичним підбором кількості колонок
- Responsive layout для форм
- Коректне відображення на мобільних та планшетах

## 🎨 Дизайн

Дизайн відповідає оригінальному HTML/CSS прототипу:
- Градієнти та тіні
- Сучасна колірна палітра
- Плавні переходи
- Картки з elevation

## 📄 Ліцензія

Навчальний проект для лабораторної роботи.